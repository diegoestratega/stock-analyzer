[project]
name = "stock-analyzer"
version = "0.1.0"
description = "FastAPI backend for Stock Analyzer"
requires-python = ">=3.9"

[project.scripts]
app = "api.index:app"

[tool.fastapi]
entrypoint = "api.index:app"
